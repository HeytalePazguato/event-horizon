/**
 * Center panel: tokens, tasks, memory, throughput, latency, tool calls.
 * @event-horizon/ui
 */

import type { FC } from 'react';

export const MetricsPanel: FC = () => {
  return <div data-metrics-panel>Metrics</div>;
};
