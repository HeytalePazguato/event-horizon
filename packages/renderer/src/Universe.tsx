/**
 * Main PixiJS canvas + React wrapper for the cosmic universe.
 * @event-horizon/renderer
 */

import type { FC } from 'react';

export const Universe: FC = () => {
  return <div data-universe aria-label="PixiJS universe canvas" />;
};
