/**
 * Pulse wave effect when task completes (radiates from singularity).
 * @event-horizon/renderer
 */

export function createPulseWave(_x: number, _y: number): void {
  // TODO: PixiJS pulse effect
}
