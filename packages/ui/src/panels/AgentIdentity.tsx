/**
 * Left panel: agent name, icon, current task, state.
 * @event-horizon/ui
 */

import type { FC } from 'react';

export const AgentIdentity: FC = () => {
  return <div data-agent-identity>Agent Identity</div>;
};
