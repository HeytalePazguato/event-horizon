/**
 * Standalone mock event server for development.
 */

import { createMockEvent } from '@event-horizon/connectors';

console.log('Event Horizon mock server (placeholder)');
console.log('Sample event:', createMockEvent());
