/**
 * Visible traffic route between agents (heavy collaboration).
 * @event-horizon/renderer
 */

export function createTrafficRoute(_fromId: string, _toId: string): void {
  // TODO: PixiJS route visualization
}
